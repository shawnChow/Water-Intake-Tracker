import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shawnchow.waterintake',
  appName: 'Water Intake Tracker',
  webDir: 'dist'
};

export default config;
